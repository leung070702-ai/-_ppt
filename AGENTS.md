# 项目协作与开发约定

## 项目目标

本项目用于构建一个面向科研竞赛场景的 PPT 辅助修改工作流：用户上传演示文稿，系统解析其结构与内容，依据用户明确的修改意图生成可审阅的修改方案，并在确认后产出新的 PPT 文件。系统的首要目标是可追溯、可回滚、不会损坏原始文件；视觉美化和复杂自动排版均不属于当前 MVP 的默认承诺。

## 当前技术栈

这是文档先行阶段，当前仓库尚未初始化前后端代码。后续 MVP 的技术基线如下：

- Python 3.11+
- FastAPI：HTTP API 层
- Pydantic v2：请求、响应、LLM 输出及领域对象的结构化校验
- `python-pptx`：PPTX 读取与有限范围的修改
- pytest：单元测试、集成测试和安全回归测试
- LLM Provider 抽象：通过环境变量配置模型与 API，不在业务代码中硬编码供应商
- 文件存储：本地隔离目录（后续可替换为对象存储）；原始上传区和生成输出区必须分离

前端、数据库、队列、向量库、复杂权限系统和多 Agent 编排暂不属于当前 MVP；除非新的里程碑明确批准，不得提前引入。

## 架构原则：单 Agent + Workflow

1. 项目使用 Skills 表达分析能力，不通过多个 Agent 拆分分析任务；确定性的文件处理由代码完成。
2. Presentation Workflow 是唯一的 Skill 编排入口，负责编排“接收请求 → 解析 PPT → 调用 Skills → 合并结果 → 生成 Edit Plan → 校验计划 → 执行修改 → 验证产物 → 返回结果”的步骤，并为每一步记录状态。
3. API 路由只处理请求和响应、鉴权边界及错误映射；业务逻辑必须进入 `service` 或 `workflow`。
4. PPT 解析和 PPT 修改必须分成不同模块，解析模块不得写文件，修改模块不得自行解释自然语言需求。
5. 默认不引入多 Agent。只有在明确需求、独立验收标准和新增测试都准备好后，才可评估改变编排方式。
6. 每次只实现一个里程碑；未完成当前里程碑的验收标准前，不开始下一个里程碑。

## Skill Architecture

本项目使用 Skills 而不是多个 Agents。每个 Skill 代表一种独立的分析能力，Skills 只由 Presentation Workflow 编排。

- Skills 必须只返回结构化输出，并且每个 Skill 对外暴露统一的 `run()` 接口。
- Skills 之间不得互相调用；Skill 不得调用其他 Skill，也不得自行编排流程。
- Skills 绝不能修改 PPT 文件、写入上传目录或执行任意文件操作。
- Presentation Workflow 合并所有 Skill 输出后，才生成最终 Edit Plan；Edit Plan 必须再次经过结构化模型和权限校验。
- 只有 `PPTEditor` 可以修改 `.pptx` 文件。`PPTEditor` 只接收已校验的 Edit Plan/命令对象，不接收自然语言指令。
- 任何其他模块（包括 API、Skill、解析器和通用 service）若需要修改 PPT，必须转交给 `PPTEditor`。

## MVP 范围

包含：

- 接收单个 `.pptx` 文件和明确的修改请求
- 读取幻灯片、文本框、表格、图片占位信息及基础版式元数据
- 将解析结果转换为稳定的结构化领域模型
- 让 LLM 生成经过模型校验的修改计划（例如目标页、目标元素、操作类型、新内容）
- 仅执行白名单内、可验证的修改操作
- 采用 copy-on-write 生成新文件，并在输出前进行 PPTX 完整性验证
- 返回任务状态、修改摘要、输出文件路径/标识和可审阅的警告

不包含：

- 覆盖原始上传文件
- 任意宏、外部链接、嵌入式 OLE 或未知二进制对象的自动改写
- 未经用户确认的批量修改
- 复杂视觉设计系统、自动配色、跨模板迁移
- 多用户协作、异步任务集群、计费、生产级对象存储和多 Agent

## 目录职责

目标目录结构（文档阶段可尚不存在）：

```text
.
├── AGENTS.md                 # 本文件：协作规则与安全边界
├── README.md                 # 项目说明、使用方式和当前状态
├── PLANS.md                  # 可验收的开发里程碑
├── .env.example              # 环境变量模板，不含任何秘密
├── app/
│   ├── api/                  # 路由、请求/响应 DTO、错误映射；不放业务逻辑
│   ├── models/               # Pydantic 模型和领域数据结构
│   ├── skills/               # 独立分析能力；每个 Skill 提供 run()，只返回结构化结果
│   ├── services/             # 文件、LLM、任务等可复用业务服务
│   ├── workflows/            # Presentation Workflow：调用/合并 Skills 和状态转换
│   └── ppt/
│       ├── parser.py         # 只读解析；禁止写入 PPT 文件
│       └── modifier.py       # 仅由 PPTEditor 执行白名单修改和产物写出
├── tests/                    # 与 app 对应的单元、集成和安全测试
├── storage/
│   ├── uploads/              # 原始上传，只读保存，禁止被修改
│   ├── work/                 # 临时副本，任务结束后按策略清理
│   └── outputs/              # 新生成 PPT 和验证报告
└── docs/                     # 设计决策、接口契约和运行手册
```

## 编码规范

- Python 使用 Ruff/Black 兼容风格，4 空格缩进；公共函数、类和模块必须有类型标注。
- 命名使用 `snake_case`（变量/函数）、`PascalCase`（类/Pydantic 模型）、大写下划线（常量）。
- 优先小模块和纯函数；副作用（文件写入、网络、LLM 调用）必须集中在边界层，Skill 内不得包含文件写入副作用。
- 禁止在路由中直接调用 `python-pptx`、LLM SDK 或 `open()` 完成业务操作。
- 错误必须使用可识别的异常类型和稳定错误码；日志不得打印 API Key、文件内容或完整提示词中的敏感信息。
- 配置从环境变量读取，代码中不得提交真实密钥、个人路径或临时调试开关。
- 变更应保持最小范围；不要添加当前 MVP 不需要的复杂基础设施。

## 测试要求

- 新功能必须有测试；先写能失败的测试，再实现最小行为，然后运行回归测试。
- 单元测试覆盖模型校验、解析器、修改器、路径安全和错误分支。
- 集成测试覆盖完整 Workflow，至少包含“解析 → 计划校验 → 修改 → 产物验证”。
- 使用固定的最小 PPTX fixture；测试不得依赖真实用户文件、线上 LLM 或未锁定的网络服务。
- 对 LLM 使用 mock 响应，另外测试无效 JSON、字段缺失、越权操作和超时/重试边界。
- 合并前运行 `pytest`；涉及格式或类型变更时同时运行项目配置的 lint/type check。

## 文件安全要求

- 不得覆盖用户上传的原始 PPT。原始文件保存后视为只读，任何修改都写入新的临时副本和新的输出路径。
- 文件名、路径和任务 ID 必须经过规范化；拒绝路径穿越、符号链接逃逸、目录覆盖和可执行扩展名。
- 仅接受明确允许的扩展名和 MIME/ZIP 结构；限制文件大小、解压后大小、页数和处理时长，防止资源耗尽。
- 临时文件使用任务专属目录；失败时清理临时产物，保留可诊断的错误记录而不泄露文件内容。
- 输出文件完成后执行 ZIP/PPTX 完整性检查；验证失败时不得向用户返回该文件。

## LLM 调用规则

- 所有 LLM 输出和 Skill 输出必须经过结构化模型校验；校验失败即停止执行，不得“猜测修复”或直接落盘。
- 使用版本化、最小权限的提示词；明确要求只返回约定的 JSON 结构，并限制可用操作、页码范围和字段长度。
- LLM 只能提出分析结果或修改计划，不能直接执行未经验证的文件操作、任意 Python、Shell 或路径操作；Skills 也不得代替 PPTEditor 写入文件。
- Workflow 在执行前再次校验目标页、元素 ID、操作白名单、内容长度和用户授权范围。
- 记录模型名、提示词版本、请求 ID、校验结果和摘要；不得记录密钥、完整文件二进制或不必要的敏感文本。
- 设置超时、有限重试和成本/Token 上限；失败时返回可理解的状态，不自动扩大权限或修改范围。

## PPT 修改安全规则

- 解析与修改严格分模块；解析器只读，Skills 只读并返回结构化结果，PPTEditor 接收已校验的命令对象。
- 修改前复制原始 PPTX；写入过程中不得触碰 `storage/uploads` 中的文件。
- PPTEditor 只实现显式白名单操作，并对每次操作记录前后摘要；遇到未知形状、宏、嵌入对象或结构异常时拒绝修改并保留原件。
- 修改后重新打开并验证页数、关键元素和文件结构；任何验证失败都丢弃输出并报告原因。
- 默认不改变主题、母版、动画、切换、备注和外部链接；如未来支持，必须单独建立里程碑和回归 fixture。

## 交付节奏

- 每次只实现一个里程碑；里程碑完成前不得顺手加入下一阶段功能。
- 新功能必须有测试，且验收标准、测试结果和已知风险必须同步更新到 `PLANS.md`。
- 未经明确要求，不引入多 Agent。
- 不要添加当前 MVP 不需要的复杂基础设施。

## 完成任务后的汇报格式

```text
完成：<里程碑或任务名称>

变更：
- <文件路径>：<做了什么>

验证：
- <命令>：<结果>
- <关键安全检查>：<结果>

未完成/风险：
- <没有则写“无”>

下一步：
- <仅提出下一个里程碑，不在本次偷偷实现>
```
