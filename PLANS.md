# 科研竞赛 PPT 修改工作流 Implementation Plan

> 本计划服务于单 Agent + Workflow 架构；每次只实现一个里程碑，并在进入下一里程碑前完成当前里程碑验收。

**目标：** 在不覆盖原始 PPT 的前提下，交付一个可审阅、可验证、可回滚的科研竞赛 PPT 修改 MVP。

**架构：** 一个受控 LLM Agent 生成结构化修改计划，Workflow 编排解析、校验、执行和验证；API 仅负责请求/响应。PPT 解析器与 PPT 修改器分离，所有文件写入集中在受控服务和隔离目录中。

**技术栈：** Python 3.11+、FastAPI、Pydantic v2、`python-pptx`、pytest，以及可替换的 LLM Provider 抽象。

## 全局约束

- 不得覆盖用户上传的原始 PPT。
- 不得让 LLM 直接执行未经验证的文件操作。
- 所有 LLM 输出必须经过结构化模型校验。
- API 路由只处理请求和响应，业务逻辑进入 service 或 workflow。
- PPT 解析和 PPT 修改必须分成不同模块。
- 每次只实现一个里程碑。
- 新功能必须有测试。
- 未经明确要求，不引入多 Agent。
- 不要添加当前 MVP 不需要的复杂基础设施。

## 里程碑 M0：项目规范与安全边界（当前）

**目标**

完成文档先行基线，让后续实现者在相同的架构、安全和验收边界内工作。

**任务**

- 创建根目录 `AGENTS.md`、`README.md`、`PLANS.md`、`.gitignore` 和 `.env.example`。
- 固化单 Agent + Workflow、解析/修改分离、copy-on-write 和结构化 LLM 校验规则。
- 定义目标目录职责、MVP 非目标、测试策略和完成汇报格式。

**验收标准**

- 五个文件均存在于仓库根目录，内容为中文且可独立阅读。
- `AGENTS.md` 覆盖项目目标、技术栈、架构原则、MVP、目录职责、编码规范、测试、文件安全、LLM 和 PPT 安全规则。
- `PLANS.md` 的每个里程碑均包含目标、任务、验收标准、测试要求和已知风险。
- 文档明确说明当前未初始化前后端代码。

**测试要求**

- 文档静态检查：确认必需文件存在、必需约束逐条出现。
- 不运行应用测试，因为 M0 不创建应用代码。

**已知风险**

- 之前确认的技术细节若发生变化，需要在实现第一个代码里程碑前更新技术基线。
- 文档约定尚未被 CI 自动检查，早期实现需人工核对。

## 里程碑 M1：项目基础骨架（已完成）

**目标**

建立可以运行、可以测试的前后端项目骨架，为后续业务里程碑提供稳定边界；本里程碑不包含 PPT 分析、LLM、数据库或真实上传。

**任务**

- 创建 `frontend/`：Next.js App Router、TypeScript strict、首页和静态 PPT 上传界面。
- 创建 `backend/`：FastAPI 应用、Pydantic 健康检查响应模型和 `GET /health` 路由。
- 创建 `backend/app/api`、`services`、`workflows`、`models`、`tests` 包边界，并配置 pytest 与 Ruff。
- 添加前后端依赖清单、运行脚本、环境变量示例和根目录忽略规则。

**验收标准**

- 前端可通过 `pnpm build` 构建，TypeScript strict 检查通过，首页与 `/upload` 路由可渲染。
- 后端可启动，`GET /health` 返回稳定的 Pydantic 校验响应。
- pytest 至少覆盖健康检查；Ruff 能检查 `app` 与 `tests`。
- 静态上传界面不执行真实文件上传，不修改任何存储目录。

**测试要求**

- 前端运行生产构建和 TypeScript 检查。
- 后端运行健康检查 API 测试和 Ruff 检查。

**已知风险**

- 当前运行环境未预装 Node/Python 命令，需要使用工作区提供的运行时路径并在联网环境安装锁定依赖。
- 尚未建立 CI；后续发布门禁在 M6 处理。

## 里程碑 M2：领域模型与隔离文件服务

**目标**

建立可验证的请求、解析结果、修改命令和任务状态模型，并实现原始上传区、工作副本区、输出区的安全边界。

**任务**

- 创建 `app/models/` 中的 Pydantic 模型：上传元数据、幻灯片摘要、修改命令、校验错误、任务状态。
- 创建文件服务，验证扩展名、MIME、大小、路径规范化和任务目录隔离。
- 实现 copy-on-write：原始文件只读保存，修改只能写入 `storage/work` 和 `storage/outputs`。

**验收标准**

- 非 `.pptx`、超限文件、路径穿越和符号链接逃逸均被拒绝。
- 原始文件哈希在处理前后不变；输出文件路径与原始路径不同。
- 所有模型的无效字段都有稳定、可测试的错误信息。

**测试要求**

- 模型边界与错误分支单元测试。
- 临时目录下的路径穿越、覆盖原文件、清理失败产物和输出隔离测试。

**已知风险**

- Windows 与 Linux 的路径、锁文件和权限行为不同，需要跨平台 fixture。
- PPTX 是 ZIP 容器，仅扩展名检查不足，必须在后续解析阶段验证结构。

## 里程碑 M3：PPT 只读解析

**目标**

将合法 PPTX 转换为稳定、与 `python-pptx` 解耦的结构化摘要，且解析过程不产生文件写入。

**任务**

- 实现 `app/ppt/parser.py`，提取页码、形状 ID、文本、表格单元格、图片占位和基础版式信息。
- 为未知形状、损坏 ZIP、宏或嵌入对象定义拒绝/警告策略。
- 使用固定 PPTX fixture 建立解析快照和字段长度限制。

**验收标准**

- 相同 PPTX 在相同版本依赖下产生稳定摘要。
- 解析器没有写入调用；损坏或含不支持对象的文件返回明确错误。
- 摘要不包含原始二进制，不泄露超出任务所需的敏感信息。

**测试要求**

- 正常文本/表格/图片 fixture 的单元测试。
- 损坏 ZIP、空演示文稿、超页数和未知对象的测试。

**已知风险**

- 不同 PowerPoint 生成器的 XML 结构和主题继承可能导致字段缺失。
- `python-pptx` 对部分高级对象支持有限，必须维持拒绝而非猜测修改。

## 里程碑 M4：结构化 LLM 计划与 Workflow 编排

**目标**

让单个 LLM Agent 仅生成经过模型校验的修改计划，并由 Workflow 串联请求、解析、计划审阅和执行前检查。

**任务**

- 创建 `app/services/llm/` 的 Provider 接口、版本化提示词和超时/重试策略。
- 定义严格的修改计划 schema：操作白名单、目标页/元素 ID、参数、理由和警告。
- 创建 `app/workflows/modify_ppt.py`，实现状态转换、幂等任务 ID 和失败终止。
- 让 API 路由只调用 Workflow 并映射响应。

**验收标准**

- 无效 JSON、字段缺失、越权页码、未知操作和超长内容均在执行前被拒绝。
- LLM 无法直接访问文件系统、Shell 或任意代码执行器。
- Workflow 每一步有可观察状态；失败不会进入修改阶段。

**测试要求**

- 使用 mock LLM 覆盖成功、校验失败、超时、重试耗尽和提示词注入样例。
- 端到端测试覆盖“解析 → 计划校验 → 执行前检查”，并断言 API 层无业务分支。

**已知风险**

- 模型输出可能包含看似合理但不存在的元素 ID，必须以解析摘要为准二次校验。
- 重试可能造成成本增加或重复请求，需要请求 ID 和预算上限。

## 里程碑 M5：白名单 PPT 修改与产物验证

**目标**

在工作副本上执行有限、可回滚的修改，并证明输出 PPTX 可重新打开且关键内容符合计划。

**任务**

- 实现 `app/ppt/modifier.py`，只接受已校验的命令对象。
- 首批支持有限文本替换、文本框新增/删除（如 fixture 证明安全）和表格单元格更新；默认不改主题、母版、动画、备注或外链。
- 增加输出 ZIP/PPTX 完整性、页数、目标元素和变更摘要验证。
- 失败时删除未验证输出，保留原始上传和诊断元数据。

**验收标准**

- 每个白名单操作都有前后状态和可审阅摘要。
- 修改前后原始文件哈希一致；输出可被解析器重新打开。
- 任一验证失败都不会返回输出文件，也不会污染上传目录。

**测试要求**

- 每个操作的成功、目标不存在、类型不匹配和异常回滚测试。
- 重新打开输出文件的集成测试，以及原始文件不可变性回归测试。

**已知风险**

- 字体、布局溢出和主题继承难以仅靠 XML 验证发现，MVP 需把视觉复核作为人工步骤。
- 保存 PPTX 时可能丢失库不支持的高级特性，遇到风险对象应拒绝写出。

## 里程碑 M6：API 封装、可观测性与发布门禁

**目标**

提供最小可用 API、可诊断日志和自动化质量门禁，同时保持 MVP 的基础设施克制。

**任务**

- 暴露上传、创建修改任务、查询状态和下载已验证输出的最小路由。
- 增加结构化日志、请求 ID、审计摘要和健康检查；敏感信息脱敏。
- 配置 pytest、lint/type check 和安全回归命令作为 CI 门禁（不引入队列或数据库）。

**验收标准**

- API 只负责协议层；业务流程可在不启动 HTTP 服务时独立测试。
- 未验证或失败任务不能下载输出；错误码和状态稳定。
- CI 在干净环境中通过全部测试和静态检查。

**测试要求**

- API 契约测试、鉴权/输入错误测试、并发任务隔离测试。
- 日志脱敏和下载权限回归测试。

**已知风险**

- 本地文件存储不适合多实例生产部署；如需扩展，必须新增独立里程碑评估对象存储和任务队列。
- LLM 供应商 API 变更可能影响结构化输出，需要锁定 schema 版本和兼容测试。

## 计划自检

- 规范要求已在 M0、M1 和全局约束中覆盖。
- 每个里程碑都列出了可执行任务、验收、测试和风险。
- M1 已创建前后端代码骨架，但仍未引入 PPT 解析、LLM、数据库或多 Agent。

## 里程碑 M1.5：Presentation Workflow 基础编排（本次完成）

**目标**

建立 PPT 分析业务工作流的可序列化状态、固定节点契约和失败安全边界；本增量不实现真实 PPT 解析、LLM 调用或文件写入。

**任务**

- 在 `backend/app/workflows/presentation_workflow.py` 定义十个节点的显式输入/输出模型、状态模型、错误类型和日志记录。
- 固化 `validate_upload → parse_presentation → parse_rubric → analyze_storyline → analyze_slides → create_edit_plan → validate_edit_plan → apply_edits → quality_check → export_result` 顺序。
- 为未实现节点提供明确 `NODE_NOT_IMPLEMENTED` 占位实现；计划校验与文件修改之间建立强制依赖门禁。
- 添加工作流单元测试，覆盖顺序、失败终止、跳过、序列化和日志脱敏。

**验收标准**

- 工作流状态可通过 `model_dump(mode="json")` 序列化。
- 任一节点失败后，后续节点均为 `skipped` 且不被调用。
- `apply_edits` 只能接收 `ValidatedEditPlan`，未完成计划校验不得进入文件写入节点。
- 节点顺序由代码常量固定，LLM/节点输出不能改变顺序。
- 默认未实现节点返回稳定的 `NODE_NOT_IMPLEMENTED` 错误码。

**测试要求**

- `backend/tests/test_presentation_workflow.py` 覆盖成功路径、占位节点失败、计划越权操作、状态序列化和安全日志字段。
- 运行 `pytest` 与 Ruff，且不依赖真实 PPTX、线上 LLM 或多 Agent。

**已知风险**

- 当前节点实现仍为占位，无法产出真实解析或修改结果；后续里程碑必须通过依赖注入接入解析器、LLM Provider 和 PPTEditor。
- 上传校验仅覆盖工作流层面的扩展名、大小和路径片段检查；完整 MIME/ZIP/符号链接隔离仍属于 M2/M3 文件安全范围。

## 里程碑 M2-M6：MVP 闭环实现（本次完成）

**完成内容**

- `backend/app/db.py` 建立 SQLite 项目、上传、任务、步骤、幻灯片、建议、revision 表。
- `backend/app/services/ppt.py` 实现 PPTX ZIP/结构校验、页数限制、宏/外链告警、只读解析和白名单字体/文字动作。
- `backend/app/services/workflow.py` 实现 11 步后台工作流、步骤状态、演示分析结果、用户确认后的 copy-on-write 导出。
- `backend/app/services/llm.py` 提供无副作用、可替换的结构化 adapter 契约和 Demo adapter。
- `backend/app/services/rubric.py` 支持 TXT/MD/CSV/PDF/DOCX 评分标准抽取。
- `backend/app/main.py` 暴露上传、任务状态、建议审阅、revision、质检和原始/导出文件下载 API。
- `frontend/app/page.tsx` 和 `frontend/app/globals.css` 实现三栏逐页审阅工作台、故事线、评分标准、建议确认、导出状态和响应式移动布局。

**验收结果**

- `backend/.venv/Scripts/python.exe -m pytest -q`：7 passed。
- Next.js `pnpm run build`：生产构建成功，TypeScript 检查通过。
- 已验证真实最小 PPTX 的上传、异步分析、建议读取、revision 导出和原始文件字节不变。
- 已验证桌面 1440×900 与移动 390×844 浏览器交互；建议接受和故事线 Tab 可用。

**已知风险**

- 当前分析使用 Demo adapter，不调用真实 LLM；接入供应商时需实现 `LLMAdapter` 并继续复用结构化契约。
- `python-pptx` 不支持复杂 SmartArt、图表重绘及所有高级对象；这些动作会保留为人工建议。
- 本地线程 runner 适合单机 MVP，不适用于多实例生产部署。
